# TRPO (Minimal)

This is a minimal, KL-penalized policy gradient trainer that approximates TRPO behavior.
It is intended as a placeholder for a true TRPO implementation.

Run the bandit example:

```bash
export PYTHONPATH=/path/to/tinyfin/python
python examples/trpo_bandit.py
```
